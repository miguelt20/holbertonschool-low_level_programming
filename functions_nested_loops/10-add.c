#include "main.h"
/**
 *add - it shows the result of an adding
 *Return: 0
 *@a: it contains a number
 *@b: it contains another number
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;

	_putchar(sum + 48);

	return (sum);

}
